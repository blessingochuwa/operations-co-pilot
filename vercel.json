{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "vite --port $PORT",
  "functions": {
    "api/*.ts": {
      "runtime": "nodejs20.x"
    }
  }
}
